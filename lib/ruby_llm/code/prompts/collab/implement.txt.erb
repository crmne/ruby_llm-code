Two agents (<%= models %>) read the workspace and agreed on the plan below. Implement it.

The developer asked for this:

<%= task %>

The plan:

<%= plan %>

Follow it. If you find while working that a step is wrong, stop and say why rather than improvising around it.
