# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# Use the released RubyLLM 2.x line.
gem 'ruby_llm', '~> 2.0'

group :development, :test do
  gem 'overcommit'
  gem 'rake'
  gem 'rspec', '~> 3.12'
  gem 'rubocop', '>= 1.0'
  gem 'rubocop-rake', '>= 0.6'
  gem 'rubocop-rspec'
end
