The coder implemented the plan. This is what changed:

<%= diff %>

Review it against the plan you agreed on. Read the changed files if the diff is not enough to judge.

Answer with either:
- LGTM: and one sentence, or
- ISSUES: and a numbered list, each with path:line and the fix.

Real problems only: bugs, steps skipped, code that will not run, style that clashes with the file around it. Not preferences.
